import SwiftUI

/// Vỏ app tối thiểu. Trong project mới, file này là file `App` Xcode tạo sẵn —
/// chỉ `ContentView.swift` mới là thứ cần dán.
@main
struct GlassReorderApp: App {
    var body: some Scene { WindowGroup { ContentView() } }
}
